{
  "name": "app-muestreo",
  "version": "0.1.0",
  "private": true,
  "main": "src/main.js",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "electron-dev": "concurrently \"npm run dev\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-build": "next build && next export && electron-builder --dir"
  },
  "dependencies": {
    "concurrently": "^7.6.0",
    "d3": "^7.8.3",
    "electron-is-dev": "^2.0.0",
    "next": "13.2.4",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "wait-on": "^7.0.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "electron": "^23.1.4",
    "electron-builder": "^23.6.0"
  }
}
